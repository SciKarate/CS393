# CS393
Currently supports:
> A master block
> a blockmap
> iNodes
> an iNodeMap
> Directories
> A rudimentary shell

Can use the shell to create folders, navigate between them, write to disk, and read from disk.
